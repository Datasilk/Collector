namespace Collector.Auth.RemoteAuthentication
{
    public enum RemoteAuthenicationResponse
    {
        Valid,
        Invalid,
        IPLocked,
        UnableToComplete,
        NotConfigured
    }
}
